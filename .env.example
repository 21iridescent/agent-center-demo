# ───────────────────────────────────────────────────────────────
# AI provider — OpenRouter（OpenAI 兼容协议，多模型聚合网关）
# 5 个 chat / tool 路由都读这一个 key（lib/deepseek.ts:14）
# ───────────────────────────────────────────────────────────────
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxx

# ───────────────────────────────────────────────────────────────
# Google Gemini — 仅 /api/generate-persona 在线生成头像/全身像用
# 用 gemini-3.1-flash-image-preview 双图并行（1:1 头像 + 3:4 全身像）
# 不开人像生成可不填；缺这个 key 时 generate-persona 路由会 500
# 申请：https://aistudio.google.com/apikey
# 文档：https://ai.google.dev/gemini-api/docs/image-generation?hl=zh-cn
#
# ⚠ 故意 NOT 用 GEMINI_API_KEY — 用户 shell rc 里可能 export 了同名变量，
# Next 的 .env.local 不覆盖已设的 process.env。改用 GOOGLE_GENAI_API_KEY
# 这个项目里独占的名字。
# ───────────────────────────────────────────────────────────────
GOOGLE_GENAI_API_KEY=AIzaSyxxxxxxxxxxxxxxxxxxxxxxxx

# ───────────────────────────────────────────────────────────────
# Exa — 备课 agent 联网工具（lib/tools/exa.ts）
# 三个工具用：web_search / crawl_url / find_similar
# 免费档 1000 调/月；缺这个 key 时联网工具 throw，UI 显示「✗ EXA_API_KEY missing」
# 但 LLM-as-tool 学科助手不依赖 Exa，仍可用
# 申请：https://dashboard.exa.ai/
# ───────────────────────────────────────────────────────────────
EXA_API_KEY=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ───────────────────────────────────────────────────────────────
# Vercel KV (Upstash for Redis)
#   生产：在 Vercel Dashboard → Storage → Create → 把 KV 数据库 link
#         到这个项目，下面 4 个会自动注入到 Production / Preview
#   本地：可手填 Upstash 控制台拿到的 REST URL/Token；不填时
#         lib/kv.ts / lib/agent-storage.ts 走模块级内存兜底
#         （demo 跑不挂，但 server 重启 / serverless 每次 invocation 全丢）
# ───────────────────────────────────────────────────────────────
KV_URL=redis://default:xxx@xxx.upstash.io:6379
KV_REST_API_URL=https://xxx.upstash.io
KV_REST_API_TOKEN=AXxxxxx
KV_REST_API_READ_ONLY_TOKEN=ArXxxxx
