# ───────────────────────────────────────────────────────────────
# AI provider — OpenRouter（OpenAI 兼容协议，多模型聚合网关）
# 5 个 chat / tool 路由都读这一个 key（lib/deepseek.ts:14）
# ───────────────────────────────────────────────────────────────
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxx

# ───────────────────────────────────────────────────────────────
# OpenAI — 仅 /api/generate-persona 在线生成头像/全身像用
# 不开人像生成可不填；缺这个 key 时 generate-persona 路由会 500
# ───────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

# ───────────────────────────────────────────────────────────────
# Vercel KV (Upstash for Redis)
#   生产：在 Vercel Dashboard → Storage → Create → 把 KV 数据库 link
#         到这个项目，下面 4 个会自动注入到 Production / Preview
#   本地：可手填 Upstash 控制台拿到的 REST URL/Token；不填时
#         lib/kv.ts / lib/agent-storage.ts 走模块级内存兜底
#         （demo 跑不挂，但 server 重启 / serverless 每次 invocation 全丢）
# ───────────────────────────────────────────────────────────────
KV_URL=redis://default:xxx@xxx.upstash.io:6379
KV_REST_API_URL=https://xxx.upstash.io
KV_REST_API_TOKEN=AXxxxxx
KV_REST_API_READ_ONLY_TOKEN=ArXxxxx
