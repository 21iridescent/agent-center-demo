# Design Brief · 智能体中心 · 全系视觉升级

> 由 `/impeccable shape` 产出。设计上下文见 `.impeccable.md` & `CLAUDE.md`。
> 这份是 **plan**，不写代码。下一步可以交给 `/impeccable craft <feature>` 逐页/逐组件实现。
>
> **Lock-in:** 路由结构不变 · 三类型语义（dialogue/debate/discuss）不变 · 现有组件名保留 · 仅桌面（min-vp 1024）

---

## 1. Feature Summary

智能体中心从"国产 SaaS 后台模板观感"升级为**"教师休息室纸感笔记本"**：在保留**学问蓝 / 辩论红 / 讨论青**三类型骨架的前提下，彻底重写**字体 + 中性色 + 排版节奏 + 布局语言 + 动效**的全系视觉重做。

覆盖 **7 个路由 + 15 个组件**：

```
/                       → Home（备课 / 授课 / 评价 三段差异化）
/prep/{lesson|exercise|activity|outline}  → 4 个备课工具页
/records                → 记录列表（长版编辑式）
/create/[kind]          → 三类型表单页

components: Topbar · HomePhase · ToolCard · AgentCard · AgentForm · AgentCreatePage
            · ChatArea · ChatInput · ContextStrip · FilterChips · PrepToolPage
            · RecordCard · ConfirmModal · SaveButton · Toast
```

---

## 2. Primary User Action

老师每次打开（备课 OR 上课投影），**3 秒内**能回答两件事：

1. 我在三段流程哪一段？（备课 / 授课 / 评价）
2. 这一段下一个最该点的按钮在哪？

不是"看到一堆功能卡"，而是"读到一段**被认真编辑过的内容**"。

---

## 3. Design Direction

### Aesthetic point of view
**An editor-designed teacher's notebook** — 把教师工具做得像一份**精心排版的工作笔记**。

- **Page = section in a book**，不是 "a screen"。每个路由 = 教科书里的一节。
- **Hierarchy from typography & edge anchors**，不是 cards & shadows。章节号 / 版口栏线 / 侧栏贴 / 页码标记。
- **Color = editorial markup**，不是 theme。三类型色像编者的红笔/蓝笔/绿笔——稀少但权威。

### Typography pairing

按 `<font_selection_procedure>` 跑过一轮：

> **Brand voice 3 words**: 温暖 + 考究 + 翻得卷边
>
> **反射性会想到的字体**（拒绝）：思源宋体 / Newsreader / Fraunces / Inter — 全在 reflex 列表里
>
> **重选**：

| 角色 | 字体 | 选它的理由 |
|---|---|---|
| 中文 Display | **霞鹜文楷 LXGW WenKai** | 开源、SIL OFL；介于楷与宋之间的现代 hand-feel；不是博物馆体也不是儿童体；字面对应"翻旧的备课本" |
| 中文 Body | **Source Han Sans SC**（保留） | 维持正文密度；与 LXGW 楷体形成 display↔body 对位 |
| 西文 / 数字 | **Source Serif 4** + tabular nums | 与 Source Han 同设计师血缘，中西混排不打架；提供 tnum / lnum / oldstyle；非 reflex 列表 |
| 标签 / 数据 | Source Serif 4 Small Caps | 不再用 sans 当 label；不在按钮里全大写堆 Inter |

**字号 scale**（fixed rem，product UI；ratio ≈ 1.25）:

```
xs    11 / 16     · meta / hint
sm    13 / 20     · body 二级
base  14 / 22     · body 主体
md    16 / 24     · 卡片标题 / 章节副标
lg    20 / 28     · 章节主标
xl    28 / 36     · 页面 H1
display 36 / 44   · 投影专用（type 章节标识）
```

### Color rewrite — OKLCH paper palette

```css
/* Paper neutrals — 朝赭/米黄微偏，chroma 0.005-0.012 */
--paper-base:   oklch(0.985 0.008 80);   /* 档案册页主底 */
--paper-soft:   oklch(0.972 0.010 80);   /* 侧栏 / inset 二级底 */
--paper-edge:   oklch(0.930 0.012 80);   /* 边线 / 分隔线 */
--paper-stamp:  oklch(0.150 0.012 80);   /* 章式深色（章节号底） */

/* Ink scale — 替代当前 --color-text-1..7 */
--ink-1: oklch(0.200 0.010 80);   /* 主文 */
--ink-2: oklch(0.420 0.010 80);   /* 二级 */
--ink-3: oklch(0.620 0.010 80);   /* 三级 / meta */
--ink-mute: oklch(0.750 0.010 80); /* hint */

/* Brand types — 保留语义，OKLCH 校准、降饱和到与纸调和谐 */
--type-dialogue:    oklch(0.620 0.180 250);  /* 学问蓝 — 比 #368FFF 更内敛 */
--type-debate:      oklch(0.590 0.210 25);   /* 辩论红 — 朝陈年红墨偏 */
--type-discussion:  oklch(0.680 0.140 220);  /* 讨论青 — 朝蓝绿过渡 */

/* Functional accents */
--accent-launch:    oklch(0.650 0.160 145); /* 启动绿 — 仅"启动"按钮 */
--accent-warn:      oklch(0.620 0.140 70);  /* 提示黄 */
```

具体 hue/chroma 在 craft 阶段对照投影显示器实测后微调。

---

## 4. Layout Strategy

### System level

**版口（Page Margin）**:
- ≥1440px: 左右各 80–120px "装订余白"
- 1024–1440px: 左右各 32–48px

**栏宽**: 三段（备课/授课/评价）走**不同栏宽**——"editorial hierarchy" 原则的具体落地：

```
① 备课  → 12 栏自适应 grid，4 个工具卡（保留密度，重做卡片语言）
② 授课  → 不对称 8/4 布局：左 2/3 agent grid · 右 1/3 侧栏（type 快建 / 配置入口）
③ 评价  → 单栏长版列表，65–75ch，章节分组（按周/月）
```

**章节号**: 不用大圆圈带号，用**版印章式**——黑底白字方形章 + 章节名 + 副标在下一行。投影 1m 仍可读。

### Page level (7 routes → 5 archetypes)

| 路由 | 原型 | 重做方向 |
|---|---|---|
| `/` | **Cover + 3 sections** | 移除"四等卡片刷三屏"，三段差异化布局；版印章节号 |
| `/prep/{lesson,exercise,activity,outline}` | **Workshop（form + output）** | 三栏：左侧栏上下文/历史 · 中表单+输出 · 右上 SaveButton。表单不再撑满整页 |
| `/records` | **Long-form list** | 去 card，改"分页索引卡 + 章节分组（按时间）"；hover 出侧栏快捷动作 |
| `/records/[id]` | legacy HTML | 本期不动（沿用 legacy 链接，列入 v2） |
| `/create/[kind]` | **Type-scoped form** | header 用对应类型色作为单一锚点；其他装饰一律纸调；类型切换 280ms 互译 |

---

## 5. Key States

**系统层**:

| 状态 | 体验目标 | 不要做 |
|---|---|---|
| Default | 三段式版面加载完成；一眼看出"现在在哪一段" | 全屏 hero |
| Empty | "你还没配置任何 [类型] 智能体"——提示行 | 占位卡片 / illustration |
| Loading（流式 AI 输出） | 段落级骨架：保留版口和章节号，仅文字 shimmer | 全屏 spinner / 跳动光圈 |
| Error | 像 errata 勘误条——黑底白字横幅，左 ⚠ 标记，可关闭 | 红色弹窗 / 大量文案 |
| Manage mode | 整体 lightness -5%，版口出现编辑工具栏 | 抖动 / 跳动提示 |
| Toast（已存在） | 投影 1m 可读，≥16px | 现状的 13px |
| Confirm Modal | 卡式贴纸贴在版面上 | 居中遮罩 modal |

---

## 6. Interaction Model

### Motion philosophy: **像翻页**

- **进入页面**: stagger reveal — 版口 → 章节号 → 文字（180ms ease-out-quart，间隔 30ms）
- **hover 卡片**: 不"浮起"。改为**侧栏小标露头**——hover 在 record 上时，左侧出现 3px 章式小标
- **点击按钮**: 1px translate-y + 颜色加深，不用 box-shadow
- **height 动画**: 全部用 `grid-template-rows: 0fr → 1fr`，禁止 `height: auto` 过渡
- **类型切换**（create/[kind]）: header color 280ms 互译；**禁止** scale / bounce

### Progressive disclosure
- 首页"快速新建" pill → 改为**贴纸标签式**：白底 + 1px 中性边 + 类型小圆点 + 文字
- AgentCard 的"启动"按钮：default 时正常宽度但是 64% lightness 的中性绿；hover 升至 100% — 不在 default 占满视觉

### 投影 hint（不做模式开关）
不需要"演示模式"按钮。**默认态在 1m 投影距离仍可读**——这是字号/对比度/间距的硬约束，不是模式切换。

---

## 7. Content Requirements

**Microcopy 重检**:

| 当前 | 候选 | 理由 |
|---|---|---|
| "打开使用" | "进入" 或 "开始" | 短动词给字号让路 |
| "▶ 启动" | "启动" + ▶ 移到按钮**外**右侧 | 按钮内只放文字；图标作为指引锚 |
| "查看全部 →" / "全部记录 →" | 保留 | "目录页脚"语言，符合编辑式 |
| section 副标（"平台 AI 工具，把课备扎实"等） | 保留并升格为**章节副标位** | 这是产品 voice 的重要资产 |
| 共 N 个 / 上次 X | 保留 | 简洁 |

**新增**:
- 章节副标位（教科书"1.2 什么是温度"那种）
- Empty state 文案 × 3（empty agents / empty records / empty prep history）
- Errata 错误条文案模板

---

## 8. Recommended Impeccable References

按 plan 实现优先级:

1. **`reference/typography.md`** — **最高优先级**。中西文混排、tabular nums、modular scale、OpenType。字体决定整盘成败。
2. **`reference/spatial-design.md`** — 不对称布局 / 栏宽 / 章节余白 / container queries。
3. **`reference/color-and-contrast.md`** — OKLCH 落地、纸调中性、投影场景对比度。
4. **`reference/motion-design.md`** — stagger / grid-template-rows / ease-out-quart。
5. **`reference/interaction-design.md`** — 备课页是 form-heavy，focus state 必须考究。
6. **`reference/responsive-design.md`** — **跳过**（仅桌面）。

---

## 9. Phased Roadmap（plan 的 plan）

把这份 brief 切成 4 个可独立交付的阶段，每段都能独立 commit / 独立 review:

### Phase A — Design System Tokens（1 个 craft session）
- 重写 `app/globals.css` 的 `@theme` 块：纸调 OKLCH 中性 + 校准三类型色
- 新增字体加载（webfont swap + 子集化策略）
- 字号 scale 从 px 变量改为语义 token（`--text-base`, `--text-lg`...）
- 间距 scale 改为 4pt-base 语义 token（`--space-sm`, `--space-md`...）
- **交付物**: 新 globals.css；token 一览页（可选 `/tokens` 路由展示）

### Phase B — Primitives（1 craft session）
重做 6 个最底层组件：
- `Topbar`（版印 brand + 章节切换）
- `HomePhase`（章节号 + 副标 + 不对称栏）
- `ToolCard` / `AgentCard`（去阴影、上侧栏标签）
- `ConfirmModal`（卡式贴纸）
- `Toast`（≥16px 投影可读）

### Phase C — Pages（2 craft sessions）
- Session 1: Home（`/`） + records（`/records`）
- Session 2: 4 个 prep tools（`/prep/*`） + create flow（`/create/[kind]`）

### Phase D — Motion & Polish（1 craft session）
- 全站 stagger reveal
- hover/focus 系统统一
- empty/loading/error 三态全部落地
- AI slop test pass（`/impeccable polish`）

**总计估算**: 5 个 craft session。Phase A 必须最先做（其余依赖它）；B 必须在 C 之前；D 收尾。

---

## 10. Open Questions（不阻塞 plan，留给 craft）

1. **正文中文字体** — Source Han Sans SC 保留 vs 全切到 LXGW WenKai Lite（Serif）。建议 Phase A 末出两份对照样本，桌面+投影实测后定。
2. **数字字体重量** — Source Serif 4 Light vs Regular，影响 stats 行（"96 条对话 · 30 人参与"）的重量感。
3. **legacy `/legacy/*.html`** — 首页和 records 仍大量链向 legacy HTML 原型。**默认假设：本期不动，链接保留**。如果纳入升级，整个 plan 再加 Phase E。
4. **Manage mode 进入方式** — 现状是按钮切 manageMode；本期是否升级为"编辑装订"全屏暗化模式？默认假设：保留按钮模式，仅视觉升级。
5. **章节号字符** — 现状是 ①②③ 带圈数字；编辑设计语言更倾向阿拉伯数字 + 章式底纹。default 是切到 1/2/3，但需你拍板。

---

*Brief generated 2026-05-08 by /impeccable shape. Hand off to: `/impeccable craft <feature>` 逐页推进。*
